<% include ../partials/header %>

	<h1>This is the companies page.</h1>
	<ul>
		<% listOfCompanies.forEach((elm) => {   %>
			<div>
				<h4>
					<%= elm.name %>
				</h4>
				<img src=<%=elm.image %> />
			</div>
			<% }); %>

	</ul>

	
<% include ../partials/footer %>